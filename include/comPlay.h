#pragma once
#include "player.h"

namespace BattleShips{
    class ComPlay: public Player
    {
        public:
            ComPlay();
            virtual ~ComPlay();

        protected:

        private:
    };
}


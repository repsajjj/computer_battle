#ifndef DEBIAN_H
#define DEBIAN_H
#include "computer.h"

namespace BattleShips{
    class Debian : public Computer
    {
        public:
            Debian();
            virtual ~Debian();

        protected:

        private:
    };
};

#endif // DEBIAN_H
